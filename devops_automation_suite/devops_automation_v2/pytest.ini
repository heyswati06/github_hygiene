[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Pure unit tests (no I/O, fully mocked)
    integration: Tests that call real GitHub API (require GITHUB_TOKEN)
    slow: Tests that take >5 seconds
